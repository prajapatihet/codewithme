const balance = new Number(100.89);


// console.log(balance.toFixed(1));

min = 10;
max =20;

// Math.random();
// It will gives random values between 0 and 1 inclusive 
// If we want to generate same specific value within some specific range then we
// use below mathematical operation.
    
console.log(Math.floor(Math.random() * (max-min + 1) + min))